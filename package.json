{
  "name": "mingli",
  "version": "0.1.1",
  "private": true,
  "description": "紫微斗数排盘 API 与可视化页面 Demo",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check:server": "node --check server.js",
    "validate:samples": "node scripts/validate-samples.js",
    "validate:work-story": "node scripts/validate-work-story.js",
    "verify": "npm run check:server && npm run validate:samples && npm run validate:work-story"
  },
  "dependencies": {
    "iztro": "^2.5.8",
    "lunar-typescript": "^1.8.6"
  },
  "engines": {
    "node": ">=18"
  }
}
